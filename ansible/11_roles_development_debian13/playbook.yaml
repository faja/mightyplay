---
- name: lfg
  hosts: all
  gather_facts: true
  vars:
    meta__run_platform_check: false
    meta__run_package_cache_update: false

  #tasks:
  #  - ansible.builtin.debug:
  #      msg: "1212 this is just a test"

  roles:
    - packages_cache_update
    #- packages_base
    #- packages_tools
    #- time
    - nftables
